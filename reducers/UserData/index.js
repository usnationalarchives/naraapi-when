
const intialState = {
  loggedIn: false,
  userName: ''
};

function userData(state = intialState, action) {
  switch (action.type) {
    default:
      return state;
  }
};

export default userData;